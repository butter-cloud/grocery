spring:
  application:
    name: app-server

  datasource:
    url: jdbc:postgresql://db-buttercookie.c5m0icyk0ujr.ap-northeast-2.rds.amazonaws.com:5432/postgres
    driverClassName: org.postgresql.Driver
    username: buttercookie
    password: mettredulce

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      platform: postgresql

logging:
  level:
    root: INFO
    com.grocery.app_server: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"